class Solution {
public:
    bool canWinNim(int n) {
        
       if(n%4==0) return false; // because there is only one condition when i will lose the game and that is if on my turn 
                                // stons are only 4 then i can choose 1,2,3 still friend will win
       return true;
    }
};
