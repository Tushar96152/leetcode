class Solution {
public:
    string replaceDigits(string s) {
        for(int i=1 ; i < s.length() ; i=i+2)
        {
            char x = ((s[i-1] - '0') + (s[i] - '0') + '0' );
            s[i] = x;
        }
            return s;
        
    }
};              
